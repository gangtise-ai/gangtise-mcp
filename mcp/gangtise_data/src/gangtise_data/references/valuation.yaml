name: valuation
title: Valuation
description: 查询估值指标（PE/PB/PS/PEG/PCF 等）及历史分位，多指标并发，输出 CSV。
parameters:
  securities:
    type: string
    description: 逗号分隔；可为证券名称、代码或拼音首字母
    required: true
  start_date:
    type: string
    description: 开始日期 yyyy-MM-dd。与 end_date 均不传时先查当天，无有效数据则向前回退至多 7 日并取最近有效日
  end_date:
    type: string
    description: 结束日期 yyyy-MM-dd。与 start_date 均不传时先查当天，无有效数据则向前回退至多 7 日并取最近有效日
  limit:
    type: integer
    description: 单次指标请求最大行数，默认 2000（接口上限以官方文档为准）
