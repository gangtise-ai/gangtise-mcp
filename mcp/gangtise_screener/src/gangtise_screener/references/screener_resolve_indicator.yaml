name: screener_resolve_indicator
title: Resolve Indicator
description: 解析指标说法，返回候选指标编码、名称与参数元数据（用于 screener 前置消歧）。
parameters:
  phrase:
    type: string
    description: 指标说法，逗号分隔可传多个
    required: true
  top:
    type: integer
    description: 每个说法返回的候选数量，默认 5
