[project]
name = "gangtise-pdf-cli"
version = "0.1.0"
description = "Gangtise CLI (gangtise_pdf)."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "Gangtise" }]
dependencies = [
    "gangtise-pdf-mcp",
    "requests>=2.32.5",
    "pyyaml>=6.0",
    "pypdf>=4.0.0",
]

[project.scripts]
gangtise-pdf = "cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
only-include = ["src"]

[tool.hatch.build.targets.wheel.sources]
"src" = ""

[tool.hatch.build.targets.sdist]
only-include = ["src", "pyproject.toml", "README.md", "LICENSE"]

[tool.uv.sources]
gangtise-pdf-mcp = { path = "../../mcp/gangtise_pdf", editable = true }
